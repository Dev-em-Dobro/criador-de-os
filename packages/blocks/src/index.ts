/**
 * @os/blocks — catálogo de blocos genéricos reutilizáveis por qualquer cliente.
 *
 * Fase 0: vazio. Fase 2 preencherá com `kpi-dashboard`, `data-table`, `kanban-board`, etc.
 * Cada bloco será registrado no formato do contrato definido em @os/core.
 */
export const BLOCKS_VERSION = '0.0.0';
